services:
  postgres:
    image: postgres:latest
    container_name: postgres_lab
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: password
      POSTGRES_DB: uczelnia_sql
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata: