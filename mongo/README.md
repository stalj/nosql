 # Launching MongoDB server
 1. Open a terminal window
 1. Move on to the mongo folder with the docker-compose.yml file
 1. Run the command:

    docker compose up -d

# Connecting to MongoDB from VS Code
Use the following connection string:

    mongodb://root:password@localhost:27017/

